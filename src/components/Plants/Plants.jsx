export default function Plants({ plants, cart, addToCart }) {
  return (
    <div>
      <h2>Available Plants</h2>

      <div className="plants-grid">
        {/* PlantCard components will go here */}
      </div>
    </div>
  );
}